schema: schema.graphqls
extensions:
  endpoints:
    prod:
      url: https://graphql.contentful.com/content/v1/spaces/${env:LEGAL_SPACE_ID}
      headers:
        Authorization: Bearer ${env:LEGAL_SPACE_TOKEN}