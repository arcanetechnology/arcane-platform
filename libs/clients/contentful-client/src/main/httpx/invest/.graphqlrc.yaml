schema: schema.graphqls
extensions:
  endpoints:
    prod:
      url: https://graphql.contentful.com/content/v1/spaces/${env:INVEST_SPACE_ID}
      headers:
        Authorization: Bearer ${env:INVEST_SPACE_TOKEN}