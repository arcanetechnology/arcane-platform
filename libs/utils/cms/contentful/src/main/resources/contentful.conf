contentful {
  spaceId = ${LEGAL_SPACE_ID}
  token = ${LEGAL_SPACE_TOKEN}
}
contentfulAlgoliaSync {
  researchArticles {
    contentful {
      spaceId = ${RESEARCH_SPACE_ID}
      token = ${RESEARCH_SPACE_TOKEN}
    }
    algolia {
      applicationId = ${ALGOLIA_APP_ID}
      apiKey = ${ALGOLIA_API_KEY}
    }
  }
}